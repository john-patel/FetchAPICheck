# name: Django CI

# on:
#   push:
#     branches: [ "master" ]
#   pull_request:
#     branches: [ "master" ]

# jobs:
#   build:

#     runs-on: ubuntu-latest
#     strategy:
#       max-parallel: 4
#       matrix:
#         python-version: [3.7, 3.8, 3.9]

#     steps:
#     - uses: actions/checkout@v3
#     - name: Set up Python ${{ matrix.python-version }}
#       uses: actions/setup-python@v3
#       with:
#         python-version: ${{ matrix.python-version }}
#     - name: Install Dependencies
#       run: |
#         python -m pip install --upgrade pip
#         pip install -r requirements.txt
#     - name: Run Tests
#       run: |
#         python manage.py test

name: Modify Frontend URLs

on:
  push:
    branches:
      - master

jobs:
  modify-urls:
    runs-on: ubuntu-latest

    steps:
    - name: Check out code
      uses: actions/checkout@v2

    - name: Modify URLs
      run: |
        # Replace "https://" with "http://" in your frontend code
        sed -i 's|https://3.111.187.209:8000/|http://3.111.187.209:8000/|g' index.html

    - name: Commit changes
      run: |
        git config --global user.name 'jhon-patel'
        git config --global user.email 'parthpatelaa19@gmail.com'
        git add .
        git commit -m 'Update backend URLs to use HTTP'
        git push

    # You can add additional steps as needed, such as deploying your frontend.
